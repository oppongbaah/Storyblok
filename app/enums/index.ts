export enum StoryVersions {
	draft = "draft",
	published = "published"
};

export enum StoriesEndpoints {
	stories = "cdn/stories",
	spaces = "cdn/spaces/me"
};

export enum Themes {
	dark = "dark",
	light = "light",
	system = "system"
}

export enum Languages {
	default = "",
	englishUS = "en-us",
	germanGerman = "de-de",
	spanish = "es"
}
